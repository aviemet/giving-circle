require "rails_helper"

xdescribe "Presentations::Interactions", type: :request do
  it "needs serializers and Inertia pages before request specs can run" do
    skip("Presentation::Interaction controllers are not wired to serializers or pages yet")
  end
end
